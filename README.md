# Promptly

**Languages:** [English](README.md) · [中文](README.zh-CN.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [العربية](README.ar.md) · [한국어](README.ko.md) · [Português](README.pt.md) · [हिन्दी](README.hi.md)

AI prompt optimization studio: turn a rough goal into a clearer prompt through Spec → Questions → Candidates → Metrics → Outcome.

Don’t want to configure an API key? Use the hosted product: **[https://promptly.solutions/](https://promptly.solutions/)**  
(also linked from Releases)

**Core product code lives on `cursor-dev`.**  
`main` is the public entry / docs branch for the repo and cloud-facing notes. Day-to-day app work happens on `cursor-dev`.

Current runnable tree: [`Promptly-cloud-v0.6.8.3-fullstack/`](Promptly-cloud-v0.6.8.3-fullstack/)

---

## Cloud vs self-host

| Path | When to use |
|------|-------------|
| **Cloud** — [promptly.solutions](https://promptly.solutions/) | Try the product without setting `OPENAI_API_KEY` / OAuth / deploy |
| **Local / self-host** | Develop, customize, or run your own backend with your own keys |

---

## Quick start (local)

Work inside the fullstack folder:

```bash
cd Promptly-cloud-v0.6.8.3-fullstack
```

### 1. Backend

```bash
cd backend
cp .env.example .env
# set at least: JWT_SECRET + OPENAI_API_KEY
npm install
npm run dev                  # http://localhost:8080 (migrations on start)
```

Minimum env for AI features:

| Variable | Purpose |
|----------|---------|
| `JWT_SECRET` | JWT signing (required in production; long random string) |
| `OPENAI_API_KEY` | OpenAI access — without it, LLM / pipeline features are disabled |
| `OPENAI_MODEL` | Optional (default in example: `gpt-4.1-mini`) |
| `CORS_ORIGIN` | Allowed frontend origin(s), comma-separated |
| `SQLITE_PATH` | Local DB (default `./data/app.db`) |
| `DATABASE_URL` | Set to use PostgreSQL instead of SQLite |
| `GROQ_API_KEY` | Optional Groq path for some inference profiles |
| `FRONTEND_URL` | Stripe / OAuth redirect base |
| `GOOGLE_*` / `GITHUB_*` | OAuth (optional) |
| `STRIPE_*` / `SUBSCRIPTIONS_ENABLED` | Billing (optional) |

Full list: [`backend/.env.example`](Promptly-cloud-v0.6.8.3-fullstack/backend/.env.example)

### 2. Frontend

```bash
cd frontend
# point API at local backend, then serve static files
VITE_API_BASE=http://localhost:8080 npm run build
python3 -m http.server 4173
```

Open `http://localhost:4173/index.html`.  
Pages read `window.PROMPTLY_API_BASE` from `config.js` (generated by `npm run build`).

### 3. Smoke checks

```bash
cd backend && npm run health
curl http://localhost:8080/api/health
```

---

## What you can do

| Feature | What it does |
|---------|----------------|
| Home / optimize | Run the multi-stage prompt pipeline with live progress |
| Brainstormer (wizard) | Guided Q&A to sharpen intent before generation |
| Specs | Spec extraction / review from goals and answers |
| Enhancer | Iterate and refine existing prompts |
| Outcome | Compare candidates with metrics / outcome runs |
| Result | Shareable result view after a run |
| Auth | Email/password + Google / GitHub OAuth |
| Usage limits | Daily usage banner / plan limits |
| Subscriptions | Stripe checkout (feature-flagged via env) |
| Analytics dashboard | Admin analytics surfaces |
| i18n | Nine locales with language selector |

---

## Tech stack

| Layer | Choice |
|-------|--------|
| Frontend | Static HTML / CSS / JS (`config.js` build injects API base) |
| Backend | Node.js ESM + Express |
| Database | SQLite locally; PostgreSQL when `DATABASE_URL` is set |
| Auth | JWT + bcrypt; Google / GitHub OAuth |
| AI | OpenAI (primary); Groq optional via `GROQ_API_KEY` |
| Billing | Stripe (optional) |
| Deploy | Render (API) + Vercel or static host (frontend) — see nested docs |

---

## Architecture (short)

```
Browser (static frontend)
    │  fetch + Bearer JWT
    ▼
Express /api/*  (auth, oauth, pipeline, specs, questions, enhance, outcome, billing, analytics…)
    │
    ├─► SQLite or PostgreSQL
    ├─► OpenAI / Groq (Spec → Questions → Agents → Metrics → Outcome)
    └─► Stripe (optional)
```

Core optimization flow: `/api/pipeline/*` (`run`, `health`, `stream/:runId`).

---

## Main pages

| Path | Purpose |
|------|---------|
| `frontend/index.html` | Landing / optimize |
| `frontend/wizard.html` | Brainstormer |
| `frontend/specs.html` | Specs |
| `frontend/enhancer.html` | Enhancer |
| `frontend/outcome.html` | Outcome / metrics |
| `frontend/result.html` | Result view |
| `frontend/settings.html` | Account / settings |
| `frontend/subscription.html` | Plans & billing UI |
| `frontend/account.html` | Account panel |

Nested product README (more deploy detail):  
[`Promptly-cloud-v0.6.8.3-fullstack/README.md`](Promptly-cloud-v0.6.8.3-fullstack/README.md)

---

## Scripts

```bash
# Backend
cd Promptly-cloud-v0.6.8.3-fullstack/backend
npm run dev       # migrate + watch
npm start         # migrate + production
npm run health    # healthcheck
npm run migrate   # migrations only
npm test          # lightweight selftest

# Frontend
cd Promptly-cloud-v0.6.8.3-fullstack/frontend
npm run build     # write config.js from VITE_API_BASE
```

---

## Branches & layout

| Item | Role |
|------|------|
| `cursor-dev` | **Active development** — deploy / product source of truth |
| `main` | Public README / entry docs (this file) |
| `Promptly-cloud-v0.6.8.3-fullstack/` | Current fullstack snapshot on `main` |
| Older `Promptly-cloud-v0.6.*` folders | Historical snapshots |
| `milestones/` | Milestone artifacts |
| `Promptly 0.6.8 Fix Bundles/` | Fix-bundle archive |

---

## License

Nested package docs mark MIT. Treat secrets (`.env`, API keys) as private — never commit them.
