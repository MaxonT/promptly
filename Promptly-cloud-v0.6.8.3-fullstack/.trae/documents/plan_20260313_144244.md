Ah, I see! You want to **input the request as JSON** to the AI model, rather than forcing the model to *output* JSON. This makes perfect sense—feeding the model structured data (e.g., `{ "user_need": "read PDF" }`) is much more stable than feeding it raw, messy text.

### Plan: Wrap User Input in JSON Structure

I will modify `backend/src/routes/pipeline.js` to implement:

**1. System Detector (Sanitization):**
   - Create `cleanInput(text)` to strip conversational fillers ("hi", "please", "can you") from the start of the input.
   - This ensures the `user_need` field contains a clean command like "read PDF" instead of "Hi, please read...".

**2. JSON Input Construction:**
   - Instead of sending the raw `userPrompt` string to the AI, I will **wrap the user's input in a JSON structure** before sending it:
     ```json
     {
       "user_need": "[cleaned_input]",
       "context": "User is requesting a task to be structured. Treat 'user_need' as a direct command."
     }
     ```
   - This explicitly frames the input as data to be processed, bypassing the "chat mode" refusal triggers.

**3. Keep Output Format as Text:**
   - I will keep the existing `chatText` call (which returns text) and the existing `SYSTEM_PROMPT` logic for outputting the "Core Goal / Constraints / Plan" list.
   - The *only* change is how the **input** is presented to the model.

This approach stabilizes the system by treating the user's request as structured data (`user_need`), preventing the AI from misinterpreting conversational noise as chat.