/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * Analytics Dashboard - Frontend Configuration Template
 * ═══════════════════════════════════════════════════════════════════════════════
 * 
 * 使用方法:
 * 1. 复制此文件到你的项目中
 * 2. 根据你的环境修改配置值
 * 3. 确保此文件在 analytics-dashboard.js 之前加载
 * 
 * 生产部署注意事项:
 * - 确保 API_BASE 指向正确的生产服务器
 * - 根据需要调整刷新间隔
 * - 自定义颜色主题以匹配品牌
 * 
 * ═══════════════════════════════════════════════════════════════════════════════
 */

const ANALYTICS_CONFIG = {
  // ═══════════════════════════════════════════════════════════════════
  // API 配置
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 后端 API 基础地址
   * 
   * 开发环境: 'http://localhost:8080'
   * 生产环境: 'https://your-api-server.com' 或 '' (同域)
   */
  API_BASE: 'http://localhost:8080',
  
  // ═══════════════════════════════════════════════════════════════════
  // 刷新配置
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 自动刷新间隔 (毫秒)
   * 
   * 推荐值:
   * - 开发/演示: 30000 (30秒)
   * - 生产低流量: 60000 (1分钟)
   * - 生产高流量: 15000 (15秒)
   * - 禁用自动刷新: 0
   */
  AUTO_REFRESH_INTERVAL: 30000,
  
  /**
   * 默认时间范围
   * 可选值: '7d', '14d', '30d', 'all'
   */
  DEFAULT_TIME_RANGE: '14d',
  
  // ═══════════════════════════════════════════════════════════════════
  // 目标配置 (用于进度条显示)
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 业务目标
   * 这些值用于计算目标进度百分比
   */
  GOALS: {
    totalUsers: 5000,      // 总用户数目标
    dailyActiveUsers: 500  // 日活用户目标
  },
  
  // ═══════════════════════════════════════════════════════════════════
  // 颜色主题配置
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 图表和 UI 颜色
   * 可以根据品牌色调整这些值
   */
  CHART_COLORS: {
    primary: '#3b82f6',    // 主色调 (蓝色) - 用于用户数据
    secondary: '#8b5cf6',  // 辅助色 (紫色) - 用于累计数据
    success: '#22c55e',    // 成功色 (绿色) - 用于会话数据
    warning: '#f59e0b',    // 警告色 (橙色) - 用于提示
    danger: '#ef4444'      // 危险色 (红色) - 用于跳出率
  },
  
  // ═══════════════════════════════════════════════════════════════════
  // 粒子背景配置
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 粒子效果参数
   * 调整这些值以获得不同的视觉效果
   */
  PARTICLE_CONFIG: {
    count: 40,       // 粒子数量 (越多越密集，但会影响性能)
    minSize: 2,      // 最小粒子直径 (px)
    maxSize: 6,      // 最大粒子直径 (px)
    opacity: 0.15    // 最大不透明度 (0-1)
  },
  
  // ═══════════════════════════════════════════════════════════════════
  // 调试配置
  // ═══════════════════════════════════════════════════════════════════
  
  /**
   * 开发/调试选项
   */
  DEBUG: {
    enabled: false,         // 是否启用调试模式
    logApiCalls: false,     // 是否记录 API 调用
    showTimestamps: true    // 是否在日志中显示时间戳
  }
};

// ═══════════════════════════════════════════════════════════════════════════════
// 环境变量覆盖 (可选)
// ═══════════════════════════════════════════════════════════════════════════════

/**
 * 如果你的构建系统注入了环境变量，可以在这里覆盖配置
 * 
 * 示例 (Vite/Webpack):
 * if (import.meta.env?.VITE_API_BASE) {
 *   ANALYTICS_CONFIG.API_BASE = import.meta.env.VITE_API_BASE;
 * }
 */

// 检查全局 window 配置 (用于运行时配置)
if (typeof window !== 'undefined') {
  // 允许通过 window.ANALYTICS_API_BASE 覆盖
  if (window.ANALYTICS_API_BASE) {
    ANALYTICS_CONFIG.API_BASE = window.ANALYTICS_API_BASE;
  }
  
  // 允许通过 window.ANALYTICS_GOALS 覆盖目标
  if (window.ANALYTICS_GOALS) {
    ANALYTICS_CONFIG.GOALS = { ...ANALYTICS_CONFIG.GOALS, ...window.ANALYTICS_GOALS };
  }
}

// 冻结配置对象防止意外修改 (生产环境建议启用)
// Object.freeze(ANALYTICS_CONFIG);

console.log('[analytics-config] Loaded with API_BASE:', ANALYTICS_CONFIG.API_BASE);
