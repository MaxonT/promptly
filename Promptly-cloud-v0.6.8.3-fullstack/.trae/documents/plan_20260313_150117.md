I have identified the CSS rules controlling the background grid in `frontend/style.css`. The current grid is defined using `linear-gradient` with low opacity (`rgba(255, 255, 255, 0.08)` for dark mode).

To make the background grid more obvious as requested, I will increase the opacity and size of the grid lines.

### Plan: Enhance Background Grid Visibility

I will modify `frontend/style.css` to:

1.  **Increase Grid Opacity**:
    *   Change `rgba(255, 255, 255, 0.08)` to `rgba(255, 255, 255, 0.15)` (approx double visibility) for dark mode.
    *   Change `rgba(0, 0, 0, 0.15)` to `rgba(0, 0, 0, 0.25)` for light mode.

2.  **Adjust Grid Size (Optional but recommended for "obviousness")**:
    *   Keep the 40px base size but potentially add a secondary larger grid (e.g., 200px) to create a "blueprint" effect, or just simply boost the contrast of the existing one.
    *   *Decision*: I will stick to boosting contrast first to avoid clutter, as "more obvious" usually means "harder to see", not "more complex".

3.  **Specific Changes**:
    *   **Dark Mode (`body`)**:
        *   Update `linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px)` -> `linear-gradient(rgba(255, 255, 255, 0.15) 1px, transparent 1px)`.
    *   **Light Mode (`[data-theme="light"] body`)**:
        *   Update `linear-gradient(rgba(0, 0, 0, 0.15) 1px, transparent 1px)` -> `linear-gradient(rgba(0, 0, 0, 0.25) 1px, transparent 1px)`.

This will make the tech-grid pattern significantly more distinct without changing the overall layout.